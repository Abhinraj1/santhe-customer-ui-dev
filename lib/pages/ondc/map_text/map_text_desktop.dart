part of map_text_view;

class _MapTextDesktop extends StatelessWidget {
  _MapTextDesktop();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('MapTextDesktop')),
    );
  }
}
