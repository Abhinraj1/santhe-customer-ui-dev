part of map_address_ondc_view;

class _MapAddressOndcDesktop extends StatelessWidget {
  const _MapAddressOndcDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('MapAddressOndcDesktop')),
    );
  }
}
