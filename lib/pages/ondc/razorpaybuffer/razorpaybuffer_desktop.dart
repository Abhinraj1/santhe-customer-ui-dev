part of razorpaybuffer_view;

class _RazorpaybufferDesktop extends StatelessWidget {
  _RazorpaybufferDesktop();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('RazorpaybufferDesktop'),
      ),
    );
  }
}
