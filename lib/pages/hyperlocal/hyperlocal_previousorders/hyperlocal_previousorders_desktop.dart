part of hyperlocal_previousorders_view;

class _HyperlocalPreviousordersDesktop extends StatelessWidget {
  _HyperlocalPreviousordersDesktop();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('HyperlocalPreviousordersDesktop')),
    );
  }
}
