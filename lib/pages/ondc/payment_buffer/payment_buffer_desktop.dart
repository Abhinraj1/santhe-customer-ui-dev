part of payment_buffer_view;

class _PaymentBufferDesktop extends StatelessWidget {
  _PaymentBufferDesktop();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('PaymentBufferDesktop')),
    );
  }
}
