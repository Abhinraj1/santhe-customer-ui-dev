part of payment_buffer_view;

class _PaymentBufferTablet extends StatelessWidget {
  _PaymentBufferTablet();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('PaymentBufferTablet')),
    );
  }
}
