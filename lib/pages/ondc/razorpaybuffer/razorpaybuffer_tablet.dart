part of razorpaybuffer_view;

class _RazorpaybufferTablet extends StatelessWidget {
  _RazorpaybufferTablet();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('RazorpaybufferTablet')),
    );
  }
}
