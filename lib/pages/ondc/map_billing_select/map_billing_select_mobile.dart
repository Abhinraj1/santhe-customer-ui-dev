part of map_billing_select_view;

class _MapBillingSelectMobile extends StatelessWidget {
  _MapBillingSelectMobile();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('MapBillingSelectMobile')),
    );
  }
}
