part of payment_success_view;

class _PaymentSuccessDesktop extends StatelessWidget {
  _PaymentSuccessDesktop();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('PaymentSuccessDesktop')),
    );
  }
}
