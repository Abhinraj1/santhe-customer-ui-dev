part of payment_success_view;

class _PaymentSuccessTablet extends StatelessWidget {
  _PaymentSuccessTablet();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('PaymentSuccessTablet')),
    );
  }
}
