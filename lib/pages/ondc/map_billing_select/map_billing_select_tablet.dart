part of map_billing_select_view;

class _MapBillingSelectTablet extends StatelessWidget {
  _MapBillingSelectTablet();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('MapBillingSelectTablet')),
    );
  }
}
