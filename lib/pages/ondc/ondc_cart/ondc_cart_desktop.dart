part of ondc_cart_view;

class _OndcCartDesktop extends StatelessWidget {
  const _OndcCartDesktop();

  @override
  Widget build(BuildContext context) {
    // ignore: prefer_const_constructors
    return Scaffold(
      body: const Center(child: Text('OndcCartDesktop')),
    );
  }
}
