part of map_billing_select_view;

class _MapBillingSelectDesktop extends StatelessWidget {
  _MapBillingSelectDesktop();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('MapBillingSelectDesktop')),
    );
  }
}
