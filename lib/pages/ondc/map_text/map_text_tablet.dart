part of map_text_view;

class _MapTextTablet extends StatelessWidget {
  _MapTextTablet();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('MapTextTablet'),
      ),
    );
  }
}
