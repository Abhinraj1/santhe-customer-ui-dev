part of error_nack_view;

class _ErrorNackDesktop extends StatelessWidget {
  _ErrorNackDesktop();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('ErrorNackDesktop'),
      ),
    );
  }
}
